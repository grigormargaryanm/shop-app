import {FC} from 'react';
import {View, Text} from 'react-native';

const Wishlist:FC = () => {
    return (
        <View>
            <Text>Wishlist</Text>
        </View>
    );
};

export default Wishlist;